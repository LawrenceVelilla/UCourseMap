{
  "extends": [
    "next/core-web-vitals", // Next.js base config
    "prettier" // Integrates Prettier, disables conflicting ESLint rules
  ],
  "plugins": [
    // Add any specific ESLint plugins here if needed later
  ],
  "rules": {
    // Add any specific project rule overrides here if needed later
    // e.g., "react/no-unescaped-entities": "off"
  }
}
